// Empty service worker to prevent 404 errors
// This is a placeholder - no PWA functionality is implemented
self.addEventListener('install', () => {
  self.skipWaiting();
});

self.addEventListener('activate', () => {
  // Do nothing
});
